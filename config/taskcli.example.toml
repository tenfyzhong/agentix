schema_version = 1

[storage]
path = "~/.local/share/taskcli/tasks.sqlite3"

[documents]
# Replace with an existing absolute Obsidian vault root containing .obsidian.
root = "/absolute/path/to/vault"
directory = "Agent Tasks"
